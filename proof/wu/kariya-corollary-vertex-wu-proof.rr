// kariya-corollary-vertex-wu-proof.rr
// Proof of the Kariya's theorem (Corollary) with the Wu's method
// using vertex formulation

Var = [x7,x6,x5,x4,x3,x2,x1]$
ord(Var)$

load("../../def/kariya-theorem-incenter.rr")$
load("../../src/wumethod.rr")$

wu_method(G, Gen, Var);

end$
