// kariya-theorem-incenter-wu-proof.rr
// Proof of the Kariya's theorem (Theorem) with the Wu's method
// using incenter formulation

Var = [x13,x12,x11,x10,x9,x8,x7,x6,x5,x4,x3,x2,x1]$
ord(Var)$

load("../../def/kariya-theorem-incenter.rr")$
load("../../src/wumethod.rr")$

cputime(1)$
for (I = 0; I < 1000; I++) { 
    wu_method(G, Gen, Var);
}
cputime(0)$

end$
